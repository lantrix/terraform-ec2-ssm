---
- name: Setup SSL & Start Rebased
  hosts: all
  become: true
  gather_facts: no

  pre_tasks:
    - raw: apt-get install -y python3-simplejson

  tasks:
    - name: install certbot
      apt: name=certbot state=latest

    - name: create letsencrypt directory
      file: name=/var/www/letsencrypt state=directory

    - name: Remove default nginx config
      file: name=/etc/nginx/sites-enabled/default state=absent

    - name: Install system nginx config
      template:
        src: templates/nginx-conf.j2
        dest: /etc/nginx/nginx.conf

    - name: Install nginx site for letsencrypt requests
      template:
        src: templates/nginx-http.j2
        dest: /etc/nginx/sites-enabled/http

    - name: Reload nginx to activate letsencrypt site
      systemd:
        name: nginx
      state: started

    - name: Create letsencrypt certificate
      shell: certbot certonly -n --webroot -w /var/www/letsencrypt --email {{ letsencrypt_email }} --agree-tos -d {{ domain_name }}
      args:
        creates: /etc/letsencrypt/live/{{ domain_name }}

    - name: Generate dhparams
      shell: openssl dhparam -out /etc/nginx/dhparams.pem 2048
      args:
        creates: /etc/nginx/dhparams.pem

    - name: Install nginx site for Soapbox
      template:
        src: templates/nginx-https.j2
        dest: /etc/nginx/sites-enabled/pleroma.nginx

    - name: Reload nginx to activate specified site
      systemd:
        name: nginx
      state: restarted

    - name: Add letsencrypt cronjob for cert renewal
      cron:
        name: letsencrypt_renewal
        special_time: weekly
        job: letsencrypt --renew certonly -n --webroot -w /var/www/letsencrypt -m {{ letsencrypt_email }} --agree-tos -d {{ domain_name }} && service nginx reload

    - name: Enable Soapbox service
      systemd:
        name: pleroma.service
        enabled: yes
        state: started
